/**
 * This is left only to cause the compiler to actually generate a lib file, because
 * compilers need cpp files, not h files, so to generate a lib file I needed this file.
 * Don't worry to much about this
 */
void random_func()
{
}
